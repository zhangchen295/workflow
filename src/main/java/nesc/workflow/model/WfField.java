package nesc.workflow.model;

public class WfField extends WfBaseField{
    public WfField(String filedText, String filedValue, String filedType) {
        super(filedText, filedValue, filedType);
    }
}
